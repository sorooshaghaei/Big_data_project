# copy this file to .env and add your postgres settings
# load it in your shell before running scripts
# cp .env.example .env
# set -a; source .env; set +a

PGHOST=34.155.143.75
PGPORT=5432
PGDATABASE=transport
PGUSER=team
PGPASSWORD=your-postgres-password
PGSCHEMA=public
